function calculateGrades(studentObj) {
    let courses = studentObj.courses ? studentObj.courses : []

    let courseGrade = {}
    for (let i = 0; i < courses.length; i++) {

    }
}


function calculateCourseGrades (courseExams) {
    let exams = courseExams.Exams ? courseExams.Exams : []

    let grade = 0
    for (let i = 0; i < exams.length; i++) {
        exams[i].Students[0]
    }
}